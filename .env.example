OPENAI_API_KEY=tu_clave_aqui
OPENAI_MODEL=gpt-4.1-mini

# URL de la API (scoring + MCP). Misma en app y agente.
API_BASE_URL=https://risk-scoring-api-o9ec.onrender.com

# Solo si quieres sobreescribir; por defecto se usa API_BASE_URL/mcp
# MCP_SERVER_URL=https://risk-scoring-api-o9ec.onrender.com/mcp

# Producción / Render: false (por defecto). OpenAI llama al MCP en la API.
# Local sin túnel: true + pip install -r 07_despliegue/api/requirements.txt
MCP_LOCAL_DIRECT=false
